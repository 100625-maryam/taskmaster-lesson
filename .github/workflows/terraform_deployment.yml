name: Terraform deployment

on:
  push:
    branches:
      - main

jobs:
  hello_world:
    name: My first job
    runs-on: ubuntu-latest
    steps:
    - name: first step
    - run:   echo hello world

